name: Build Linux binary

on:
  workflow_call:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install system packages
        run: |
          sudo apt-get update
          sudo apt-get install -y python3 python3-venv zip \
            libegl1 libgl1 libxkbcommon0 libfontconfig1 \
            libpulse0 libxcb-cursor0
          # ALSA lib was renamed libasound2 -> libasound2t64 on new Ubuntu images.
          sudo apt-get install -y libasound2t64 || sudo apt-get install -y libasound2

      - name: Create build venv
        run: |
          python3 -m venv .venv
          .venv/bin/python -m pip install --upgrade pip
          .venv/bin/python -m pip install -r requirements.txt

      - name: Download static ffmpeg
        run: |
          curl -fsSL --retry 5 --retry-all-errors --connect-timeout 20 \
            https://johnvansickle.com/ffmpeg/releases/ffmpeg-release-amd64-static.tar.xz \
            -o ffmpeg-release-amd64-static.tar.xz
          # Integrity check: must be a real XZ archive (magic bytes fd 37 7a 58 5a 00)
          head -c 6 ffmpeg-release-amd64-static.tar.xz | grep -q $'\xfd7zXZ\x00' || {
            echo "ERROR: downloaded file is not a valid XZ archive"
            file ffmpeg-release-amd64-static.tar.xz || true
            exit 1
          }
          tar -xJf ffmpeg-release-amd64-static.tar.xz
          mkdir -p ffmpeg/linux
          cp ffmpeg-*-amd64-static/ffmpeg  ffmpeg/linux/ffmpeg
          cp ffmpeg-*-amd64-static/ffprobe ffmpeg/linux/ffprobe
          chmod +x ffmpeg/linux/ffmpeg ffmpeg/linux/ffprobe
          rm -f ffmpeg-release-amd64-static.tar.xz

      # johnvansickle's static builds no longer ship ffplay (since 7.0.x),
      # so pull that single binary from BtbN's static GPL builds instead.
      - name: Download static ffplay
        run: |
          curl -fsSL --retry 5 --retry-all-errors --connect-timeout 20 \
            https://github.com/BtbN/FFmpeg-Builds/releases/download/latest/ffmpeg-master-latest-linux64-gpl.tar.xz \
            -o ffmpeg-master-latest-linux64-gpl.tar.xz
          head -c 6 ffmpeg-master-latest-linux64-gpl.tar.xz | grep -q $'\xfd7zXZ\x00' || {
            echo "ERROR: downloaded file is not a valid XZ archive"
            file ffmpeg-master-latest-linux64-gpl.tar.xz || true
            exit 1
          }
          tar -xJf ffmpeg-master-latest-linux64-gpl.tar.xz
          FFPLAY_SRC=$(find ffmpeg-master-latest-linux64-gpl -type f -name ffplay | head -1)
          if [ -z "$FFPLAY_SRC" ]; then
            echo "ERROR: ffplay not found in BtbN archive"
            exit 1
          fi
          cp "$FFPLAY_SRC" ffmpeg/linux/ffplay
          chmod +x ffmpeg/linux/ffplay
          rm -f ffmpeg-master-latest-linux64-gpl.tar.xz

      - name: Build single-file binary
        run: .venv/bin/pyinstaller VidGrab.spec --noconfirm

      - name: Verify binary
        run: file dist/VidGrab

      - name: Selftest (multimedia availability in packaged binary)
        run: |
          QT_QPA_PLATFORM=offscreen dist/VidGrab --selftest | tee /tmp/vidgrab-selftest.log
          grep -q "multimedia=1" /tmp/vidgrab-selftest.log

      - name: Create compressed archive
        run: tar -cJf dist/VidGrab-linux.tar.xz -C dist VidGrab

      - name: Show artifact sizes
        run: ls -lh dist

      - uses: actions/upload-artifact@v4
        with:
          name: VidGrab-linux
          path: |
            dist/VidGrab
            dist/VidGrab-linux.tar.xz